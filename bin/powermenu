#!/bin/sh
COMMANDS="
suspend\n
reboot\n
halt\n
lock\n
logout\n
"

command=$(echo $COMMANDS | \
    dmenu-dark -p "power")
[ "$command" = "" ] && exit 1
command=$(echo $command | sed 's/^ *//')
$HOME/.i3/${command}.sh && exit 0
